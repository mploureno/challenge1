<div class="form">
  <h1 class="form__legend"><%= t('.legend') %></h1>
  <%= render 'form', client: @client, url: client_path(@client) %>
  <%= link_to clients_path, class: 'btn btn--link' do %>
    <i class="fa fa-angle-left" aria-hidden="true"></i>
    <%= t('.back') %>
  <% end %>
</div>